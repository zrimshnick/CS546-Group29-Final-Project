console.log("CONNECTED TO REGISTER PAGE CLIENT SIDE JS");

const loginErrorElement = document.getElementById("loginError");
const usernameInputElement = document.getElementById("username");
const passwordInputElement = document.getElementById("password");

/* document.addEventListener("DOMContentLoaded", function () {
  if (
    loginErrorElement.textContent === "Either the username or password is wrong"
  ) {
    usernameInputElement.classList.add("loginError");
    passwordInputElement.classList.add("loginError");
  }
}); */
